"""Task Timer Package Initialization.

This module initializes the Task Timer package and provides access to the main function.

The Task Timer application allows users to track time spent on tasks, start and stop tasks, 
view summaries, edit timesheets, and check currently running tasks.

Modules:
- __main__: Contains the main function to run the Task Timer application.

Author: Nathan Baldwin
Date: February 9, 2025
"""

from task_timer.__main__ import main
